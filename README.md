# convert-doc

Convert documents into TREC format.

```
convert_doc 0.1.0
Binsheng Liu <441707+binshengliu@users.noreply.github.com>
A tool for converting collection format.

USAGE:
    convert_doc [OPTIONS] <INPUT>...

FLAGS:
    -h, --help       Prints help information
    -V, --version    Prints version information

OPTIONS:
    -o, --output <DIRECTORY>    Specify the output directory [default: .]

ARGS:
    <INPUT>...    Specify files to convert
```
